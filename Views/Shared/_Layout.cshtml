<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - CuidaPet</title>
    <link rel="stylesheet" href="~/css/style2.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="icon" type="image/png" href="/imagens/icon.png"> @* Adicionando um ícone para a aba do navegador *@
</head>
<body>
    <header class="main-header">
        @* Adicionando uma classe para estilização do cabeçalho global *@
        <div class="header-content">
            @* Novo div para organizar o conteúdo do cabeçalho *@
            <div class="cabecalho-esquerda">
                <div class="logo">
                    <img src="~/imagens/logo.png" alt="Logo CuidaPet">
                </div>
                <h1>CuidaPet</h1>
            </div>
            @* Adicionar aqui elementos do cabeçalho global, como o "JOSE" e o menu de usuário, se aplicável, no futuro *@
            <div class="header-right-actions">
                @* Exemplo de um link de sair, pode ser ajustado conforme a autenticação *@
                <a asp-controller="Login" asp-action="Sair" class="btn btn-outline-light header-logout-btn">
                    <i class="fas fa-sign-out-alt"></i> Sair
                </a>
            </div>
        </div>
    </header>

    <div class="main-content-wrapper">
        @RenderBody()
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2025 - CuidaPet - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacidade</a>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>